# LunyScript

'jQuery for Game Engine Logic"

=> Write portable game logic code. Bypass engines' highly unique APIs, their unique SceneGraphs, their unique Asset and math types. Free yourself from vendor lock-in.




´´´
When(Collision.Enter("exit"), Audio.Play("success"), Wait.Seconds(3), Stage.Load("level2"));
´´´
