source "https://rubygems.org"

# GitHub Pages gem - keeps versions in sync with GitHub Pages
gem "github-pages", group: :jekyll_plugins

# Additional plugins
group :jekyll_plugins do
  gem "jekyll-tabs"
end

# Windows-specific gems
gem "wdm", ">= 0.1.0" if Gem.win_platform?
gem "webrick", "~> 1.8"
